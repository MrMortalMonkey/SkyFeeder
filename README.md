# SkyFeeder
A HACS-installable Home Assistant integration that connects to a local tar1090-based ADS-B feeder (Ultrafeeder, readsb, dump1090-fa, graphs1090, …) and exposes everything it sees as native HA entities — plus optional sidebar panels for the map and station stats.
